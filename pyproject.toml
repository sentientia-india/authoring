[project]
name = "sentientia-course-mcp"
version = "0.1.0"
description = "Secure MCP server for e-learning course generation"
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.7.0",
  "fastmcp>=2.0.0",
  "python-dotenv>=1.0.1"
]

[project.optional-dependencies]
dev = [
  "pytest>=8.2.0",
  "ruff>=0.5.0",
  "bandit>=1.7.8",
  "pip-audit>=2.7.0"
]

[tool.ruff]
line-length = 100
src = ["src", "tests"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
