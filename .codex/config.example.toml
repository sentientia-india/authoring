# Example only. Copy to .codex/config.toml after the project is trusted locally.
# Do not commit .codex/config.toml or real tokens.

[mcp_servers.samrat-course-mcp]
url = "http://127.0.0.1:8777/mcp"
transport = "http"

[mcp_servers.samrat-course-mcp.headers]
authorization = "Bearer replace-with-local-token"
